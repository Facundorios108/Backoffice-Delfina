[build]
  command = "rm -rf node_modules .npm package-lock.json && npm cache clean --force && npm install --force && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_CONFIG_LOGLEVEL = "verbose"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
